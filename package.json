{
  "name": "cex-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"  // Add a script for development with nodemon
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Backend for CEX with Binance market data mirroring and real-time updates.",
  "dependencies": {
    "node-cron": "^3.0.2",                     // For scheduling tasks
    "bcryptjs": "^3.0.2",                       // For password hashing
    "cors": "^2.8.5",                           // For enabling Cross-Origin Requests
    "mongoose": "^7.6.1",                       // For MongoDB ORM
    "dotenv": "^16.5.0",                        // For environment variables
    "morgan": "^1.10.0",                        // For request logging
    "express": "^5.1.0",                        // Web framework
    "express-rate-limit": "^7.5.0",             // Rate limiting requests
    "helmet": "^8.1.0",                         // For setting security headers
    "jsonwebtoken": "^9.0.2",                   // For JWT-based authentication
    "nodemailer": "^6.10.1",                    // For sending emails
    "pg": "^8.15.6",                            // PostgreSQL support (not needed if only using MongoDB)
    "pg-hstore": "^2.3.4",                      // PostgreSQL support (not needed if only using MongoDB)
    "sequelize": "^6.37.7",                     // Sequelize ORM (not needed if only using MongoDB)
    "socket.io": "^4.8.1",                      // For real-time WebSocket communication
    "axios": "^1.9.0",                          // For making HTTP requests (Binance API calls)
    "nodemon": "^2.0.22",                       // For automatic server restart during development
    "ip-blocker": "^1.0.0"                      // For blocking IPs (ensure you have the correct package)
  }
}
